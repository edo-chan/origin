# Improve build performance and handle problematic packages
force=false
legacy-peer-deps=true
strict-peer-deps=false

# Skip optional dependencies that cause build issues
optional=false

# Use npm cache for better performance
cache=/tmp/.npm

# Configure for Alpine Linux builds
target_platform=linux
target_arch=arm64
python=/usr/bin/python3